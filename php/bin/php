#!/bin/bash

DIR="$(dirname "$(readlink -f "$0")")"

export PHPRC="$DIR/../etc/php.ini"
export PHP_INI_SCAN_DIR="$DIR/../etc/conf.d"
export LD_LIBRARY_PATH="$DIR/../lib:${LD_LIBRARY_PATH:-}"

exec "$DIR/php8.3" "$@"
